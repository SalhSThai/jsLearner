let text = `wiget with id `;
text.indexOf(`widget`);
text.lastIndexOf(`get`);
text.includes(`with`)
text.startsWith(`wid`);
text.endsWith(`id`);
let start =0
let end = 5 
let test = `String`;
test.slice(start,end) //ตัวสถดท้ายไม่เอา

test.substring()// index to  index
test.substr();//index,เอากี่ตัว